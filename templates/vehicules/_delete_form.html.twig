<form method="post" action="{{ path('app_vehicules_delete', {'id': vehicule.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir le supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicule.id) }}">
    <button class="btn">Delete</button>
</form>
