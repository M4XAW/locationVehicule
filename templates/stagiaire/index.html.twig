{% extends 'base.html.twig' %}

{% block title %}Stagiaire index{% endblock %}

{% block body %}
    <h1>Stagiaire</h1>

    <table class="table table-striped">
        <thead>
            <tr class="table-dark">
                <th>Nom</th>
                <th>Ville</th>
                <th>DateInscription</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stagiaire in stagiaires %}
            <tr>
                <td>{{ stagiaire.nom }}</td>
                <td>{{ stagiaire.ville }}</td>
                <td>{{ stagiaire.dateInscription ? stagiaire.dateInscription|date('Y-m-d') : '' }}</td>
                <td>
                    <a class="btn btn-outline-success" href="{{ path('app_stagiaire_show', {'id': stagiaire.id}) }}">show</a>
                    <a class="btn btn-outline-success" href="{{ path('app_stagiaire_edit', {'id': stagiaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-outline-success" href="{{ path('app_stagiaire_new') }}">Create new</a>
{% endblock %}
